
GOOGLE_API_KEY=your_google_api_key_here
GROQ_API_KEY=your_groq_api_key_here
# MODEL_SERVICE_URL: URL of the trained ResNet-18 inference service
# (psoriasis_ml/service/inference_server.py). Defaults to http://localhost:8001
# if unset. This is the PRIMARY classifier for /api/analysis/create; Gemini is
# used only for optional descriptive-text enrichment, or as a full fallback if
# this service is unreachable.
MODEL_SERVICE_URL=http://localhost:8001

# JWT_SECRET: required in production. Do not rely on the built-in fallback.

